
export class Data {

    shaders = {
        default:{ vert: "default.vert", frag: "default.frag" }
        //e.g.  shader_name: { vert: "...", frag: "..." }
    };

    textures = {
        //{{textures}}
        //e.g.  texture_name: "path"
    };

    sounds = {
        //{{sounds}}
        //e.g.  sound_name: "path"
    }

}
