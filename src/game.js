
export class Game {

    // Called when the current scene changes.
    static start(engine /*engine.js:Engine*/) {

    }

    // Called every frame after inputs have been received.
    static update(engine /*engine.js:Engine*/) {

    }

    // Called every frame right before rendering.
    static late_update(engine /*engine.js:Engine*/) {

    }

}
